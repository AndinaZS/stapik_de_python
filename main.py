from functions import *

if __name__ == '__main__':
    get_result('test.csv')

    # sales_data = chek_path("test.csv")
    # values_per_product = total_sales_per_product(sales_data)
    # values_per_data = sales_over_time(sales_data)
    # print(f"Наибольшую выручку нам принесли {get_max_values(values_per_product)}.")
    # print(f"Самый результативный день по выручке {get_max_values(values_per_data)}.")
